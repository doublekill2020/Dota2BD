<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/item_row_image_size"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="5dp">

    <ImageView
        android:id="@+id/img"
        android:layout_width="@dimen/item_row_image_size"
        android:layout_height="@dimen/item_row_image_size" />

    <TextView
        android:id="@+id/name"
        android:layout_width="70dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:maxLines="2"
        android:singleLine="false"
        android:text="Aghanim's Scepter"
        android:textColor="@android:color/white"
        android:textSize="@dimen/item_receipt_textSize"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/cost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/gold_small"
        android:gravity="center"
        android:lines="1"
        android:singleLine="false"
        android:text="3000"
        android:textSize="@dimen/item_receipt_textSize"
        android:textStyle="bold" />
</LinearLayout>